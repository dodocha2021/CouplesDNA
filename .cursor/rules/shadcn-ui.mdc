## UI Standards

### Dropdown Menu Background

**All dropdown/popover content components MUST use white background (`bg-white`) for readability.**

Affected components:
- SelectContent
- DropdownMenuContent  
- PopoverContent
- ContextMenuContent
- TooltipContent

Replace `bg-popover` with `bg-white` in the className.
